# 08 批次源码基线

本提交发布 2026-09-07 的 08 批次对应多阶段编译源码，来自 2026-09-08 singlecall 改动前冻结的工作区。三个核心编译文件 SHA-256 与 08 批次记录一致。

正式流程：意图解析 → 定向素材理解 → LLM Context-IR 草稿 → 程序编译 → LLM 最终写作 → 输出请求。

冻结时已存在的 compact_writer 等试验辅助模块保留，但不作为公开 API 的执行路径。本基线不包含后来新增的 single_call_compiler、09/10 批次测试入口或 singlecall.rules.v2。

线上四路对比页面及既有媒体保持不变。不包含环境文件、密钥、本地生成日志和新增素材。

标签：baseline-08-20260907。08 是测试批次名称，不是模型版本。
